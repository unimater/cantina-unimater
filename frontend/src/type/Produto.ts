export interface Produto { // TODO (alterar atributos)
  id: number;
  descricao: string;
  valor: number;
  situacao: boolean;
  imagem?: string;
  // categoria: Categoria;
  // categoriaId: string;
  createdAt: string;
  updatedAt?: string
}
